{
  "extends": "solhint:recommended",
  "rules": {
    "explicit-types": ["error", "implicit"],
    "gas-custom-errors": "off",
    "one-contract-per-file": "off",
    "func-visibility": ["error", { "ignoreConstructors": true }]
  }
}
