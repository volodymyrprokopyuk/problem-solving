library(lobstr)

# df <- data.frame(runif(3), runif(3))
# names(df) <- c(1, 2)
# # df[[3]] <- df[[1]] + df[[2]]
# df$`3` <- df$`1` + df$`2`

# x <- runif(1e6)
# obj_size(x)
# y <- list(x, x, x)
# obj_size(y)

# a <- c(1, 5, 3, 2)
# b <- a
# b[[1]] <- 10

# x <- c(1, 2, 3)
# obj_addr(x)
# y <- x
# obj_addr(y)

`_a` <- 1
`_a`
